# TRANSURBAN - conexão PostgreSQL do Supabase
# Copie este arquivo para backend/.env e substitua o valor abaixo.
# Use a string de conexão mostrada em Supabase > Connect > PostgreSQL.
DATABASE_URL=postgresql://USUARIO:SENHA@HOST:PORTA/postgres
